taymyr.lagom.metrics {
  graphiteReporter = {
    prefix = "prefix"
    host = "localhost"
    port = 1000
    type = "TCP"
    period = 60
    periodUnit = "MILLISECONDS"
    batchSize = 1000
    rateUnit = "MILLISECONDS"
    durationUnit = "SECONDS"
  }
}